[package]
name = "sprout_engine_unified"
version = "0.3.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.release]
opt-level = "s"

[dependencies]
bevy = { version = "0.15", default-features = true }
bevy_egui = "0.31"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
